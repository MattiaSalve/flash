[project]
name = "flash"
version = "0.1.0"
description = "Generate Anki cloze deletion flashcards from sentences in any language"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0",
    "deep-translator>=1.11",
]
